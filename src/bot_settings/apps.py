from django.apps import AppConfig


class BotSettingsConfig(AppConfig):
    """Base configuration for bot_settings app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "bot_settings"
